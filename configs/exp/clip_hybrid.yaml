# @package _global_

defaults:
  - override /model: hybrid
  - override /model/network/backbone: openclip_B_32
  - override /model/network/mid: mlp_classif
  - _self_

class_name: 'adaptive_quadtree_10_5000'
is_baseline: false
max_epochs: 30

model:
  optimizer:
    optim:
      lr: 0.0002
      weight_decay: 0.0001

dataset:
  global_batch_size: 2048
